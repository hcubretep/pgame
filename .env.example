OPENAI_API_KEY=sk-your-key-here

# Optional: Google Calendar integration
# Without this, "Sync Calendar" uses GPT to generate realistic tasks
# To get a token: use Google OAuth playground or a service account
GOOGLE_ACCESS_TOKEN=ya29-your-google-access-token
